# gaveldrop

See **[`AGENTS.md`](AGENTS.md)**. It holds the instructions for every coding agent,
this one included.

Kept as a pointer rather than a copy: two files with the same rules drift, and the
drift is silent.
