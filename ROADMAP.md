# Roadmap

Batches, in the order they are meant to land. Each batch ships something usable on its
own, and each gets its own plan before any code is written.

The order between batches is deliberate and explained where it is not obvious. The order
*within* a batch is not a commitment.

`ARCHITECTURE.md` carries the reasoning and the invariants; this file only tracks state.

---

## Lot 0 — Foundations · **done**

- [x] `ARCHITECTURE.md`: code map, invariants, placement rule
- [x] `CONTRIBUTING.md`: TDD rhythm, three gates, code conventions, no-comments rule
- [x] `AGENTS.md`, with `CLAUDE.md` as a pointer rather than a copy
- [x] `MIT OR Apache-2.0`, Apache text taken from `apache.org`
- [x] Toolchain pinned, MSRV declared
- [x] CI: three gates on Linux, tests on macOS
- [x] Conventional Commits enforced in CI by `committed`
- [x] `git-cliff` and `cargo-release` configured, pinned through `mise`
- [x] `mise run gates` and the other tasks
- [x] `main` protected, administrators included, linear history required

## Lot 1 — The fake engine · **done**

`gaveldrop-fake`, a library and a binary, depending on nothing else in the repository.

- [x] `Match`: `bin`, `args_contain`, `stdin_contains`, `call: N`, catch-all
- [x] Catch-all missing is a **load** error, not a tolerated defect
- [x] Persistent per-key call counter, key supplied by the caller
- [x] Append-only call journal, unconditional, `catch_all` and `passthrough` flagged
- [x] Static response mode
- [x] `exec: real` passthrough, skipping our own executable **by file identity**
- [x] `exec: <script>` delegation
- [x] `render: <script>` shaping, with the rule's `exit` surviving the hook
- [x] The binary, 15 end-to-end tests including 16 concurrent processes

## Lot 2 — The core, walking skeleton · **done**

Few expectations, but the whole path: YAML in, verdict out.

- [x] Case format, strict outside `setup`, open inside it
- [x] JSON schema derived from the type, committed, regenerated by a test
- [x] Isolation: temporary root, `HOME` and `XDG_*` redirected, `PATH` prefixed, symlinks
- [x] Variables that could bypass isolation are **removed**, not overridden
- [x] `Observations`, `Adapter` trait, `process` adapter
- [x] `expect.exit_code`
- [x] `expect.stdout` / `expect.stderr` with `contains` and `absent`
- [x] `expect.calls` counts, including a declared `0`
- [x] Every failure carries its assertion path
- [x] Weighted score, `allow_fail` counted apart
- [x] Terminal report, emitted per case as it finishes
- [x] `gaveldrop.yaml`, discovery sorted, empty is an error
- [x] `gaveldrop` command-line facade, with no logic of its own
- [x] gaveldrop runs its own cases through gaveldrop

---

## Lot 3 — The full expectation surface · **done**

The surface a real project needs before it can replace a hand-written harness.

- [x] Tree snapshot after `setup`, diff after invocation
- [x] `expect.files` per path, with `contains` and `absent`
- [x] Failure report lists files written that the case says nothing about — as help, not as
      an error
- [x] Structured event extraction: JSON lines on standard output, order as a subsequence
- [x] `expect.event_counts` per type
- [x] Four named invariant shapes: *paired*, *exactly one*, *no orphan*, *non-empty field*
- [x] Invariants named and parameterised in the project config, used by name in a case
- [x] `setup.exec` hook: receives the `setup` block as JSON, prepares the isolated directory
- [x] `expect.exec` hook: receives the observations as JSON, returns `{ok, diffs}`
- [x] The hook protocol documented, with a worked example in shell

Placed first among the remaining batches because it is what makes the tool sufficient
rather than merely working. Four shapes, not five: exactly the ones the prototype had.

## Lot 4 — Reports and conformance

- [ ] JSON report: a list of outcomes plus a computed summary, mergeable by concatenation
- [ ] Outcomes streamed to the JSON report as they finish, not only at the end
- [ ] HTML report, carried over from the prototype
- [ ] Conformance kit: the battery every adapter must pass
- [ ] Conformance kit checks isolation did not leak, `Observations` is filled, the catch-all
      trips, the journal is complete
- [ ] Documented as the way a third party validates their own adapter

The conformance kit belongs here rather than earlier: it puts the `Adapter` trait under
tension, and until the shell arrives that trait has one implementor.

## Lot 5 — The shell adapter

The arbiter of genericity. If the core absorbs a radically different technology without
deforming, it is generic; otherwise we learn it here, on a second real consumer.

- [ ] Source an `rc` file, invoke a function, observe files written outside the repository
- [ ] Decide how the invocation happens: full interactive load, or selective module load
- [ ] A case reproducing the k9s unresolved-variable bug of July 2026
- [ ] `idempotent: true`, or the decision that it is expressed some other way
- [ ] Whatever the core had to grow, recorded in `ARCHITECTURE.md`

## Lot 6 — The web

The batch that adds the most machinery, which is why it comes after two technologies have
already been through the core.

- [ ] A living subject: start, wait until ready, stop cleanly, reserve a free port
- [ ] Multi-step cases: a list of exchanges rather than one invocation
- [ ] A second door for fakes: an HTTP server instead of a binary on `PATH`
- [ ] The same rule engine behind that door, journal included
- [ ] REST expectations: status, headers, body
- [ ] GraphQL expectations, including `absent` over a response body
- [ ] Carry a value between steps — **and the line beyond which it stops**
- [ ] A decision on an API's database: delegated to the setup hook, or something better

## Lot 7 — Continuous integration

These formats only make sense once there are real failures to report, and by then we know
what a report must carry.

- [ ] JUnit XML
- [ ] Assertion paths resolved to line numbers
- [ ] Pull-request annotations pointing at the failing case's line
- [ ] Gating: minimum weighted score, `allow_fail` tolerance, immediate failure above a
      weight
- [ ] Case selection and sharding across machines
- [ ] Report merging exercised by a real sharded run
- [ ] A `gaveldrop/action` repository, thin, on top of the binary
- [ ] Per-environment override for `passthrough`, so CI can fake what the laptop runs for
      real

## Lot 8 — Distribution and editor integration

- [ ] Reserve `gaveldrop` on crates.io, npm and GitHub
- [ ] Publish the crates and the binary
- [ ] Publish the schema at a stable URL
- [ ] Integration documentation for a third-party project
- [ ] Watch mode: rerun affected cases on save
- [ ] Machine-readable case discovery, for editor test interfaces
- [ ] Editor plugins, outside this repository, consuming discovery and the outcome stream
- [ ] Per-ecosystem convenience packages for the hooks — each only once a real script has
      become ugly

Writing a case is already covered in any editor that speaks the YAML language server
protocol, purely because the schema is generated and committed. A plugin only adds running
and visual feedback.

---

## Outside this repository

- [ ] armadai rewrites `fake-claude` on top of `gaveldrop-fake`, keeping its `stream-json`
      shaping, with `Match` flattened under its own `agent` criterion
- [ ] armadai's harness moves to gaveldrop and its nine cases migrate — the non-regression
      proof, and it belongs to armadai rather than here
- [ ] zanvil adopts gaveldrop for the shell

## Known gaps, accepted for now

Each of these is a decision rather than an oversight. Written down so they are met as
documentation.

- [ ] A hook is resolved against the project root but runs in the isolated root. Two
      directories, documented in `docs/hooks.md` — but a reader who only skims the case format
      could reasonably expect one.
- [ ] A case cannot declare that it **expects** the catch-all to fire: any catch-all hit
      fails the case, by design. The catch-all's own semantics are therefore only testable
      from Rust.
- [ ] The lowercase commit subject is not machine-checked. `committed` can require
      capitalisation or skip the check, not require the opposite.
- [ ] The declared MSRV is not verified by CI, which runs on the pinned toolchain.
- [ ] `.github/workflows/commits.yml` references `crate-ci/committed@master`, unpinned —
      what the tool's own project documents.
- [ ] The three gate commands live in both `.mise.toml` and `ci.yml`. CI keeps one step per
      gate so a failure says which one broke.
- [ ] No test coverage threshold, deliberately.

## Not planned

- **Windows.** Isolation rests on symlinks, `PATH` and Unix-style configuration
  directories. Not an adjustment, another project.
- **Cases written in code.** An API for hooks, never for cases. The moment cases are code
  they stop being cheap, and coverage goes with them.
- **A YAML DSL.** No conditionals, no loops, no interpolation. Logic leaves the YAML and
  goes into an executable.
