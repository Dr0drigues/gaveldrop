{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Case",
  "description": "One test: how to invoke the subject, how its dependencies must respond, and what the\nresult must contain.",
  "type": "object",
  "properties": {
    "allow_fail": {
      "description": "A known failure that must not fail the whole suite. Opt in deliberately; it is\nnever inherited by omission.",
      "type": "boolean",
      "default": false
    },
    "expect": {
      "description": "What the run must produce.",
      "$ref": "#/$defs/Expect"
    },
    "fake": {
      "description": "How the faked dependencies must respond. Omit it when the subject calls nothing.",
      "anyOf": [
        {
          "$ref": "#/$defs/Scenario"
        },
        {
          "type": "null"
        }
      ]
    },
    "name": {
      "description": "Human-readable name. Also the identifier in reports, so make it say what the case\nproves rather than what it does.",
      "type": "string"
    },
    "setup": {
      "description": "How to prepare and invoke the subject.",
      "$ref": "#/$defs/Setup"
    },
    "steps": {
      "description": "Exchanges with the subject, in order, each with its own expectations.\n\nInvoking a subject more than once is observable of any process — you can run a binary\ntwice — so this is part of the format rather than one technology's vocabulary. `expect`\nabove still describes what the run produced **as a whole**; a step describes one exchange.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/Step"
      }
    },
    "weight": {
      "description": "How much this case matters. Reports sort failures by it, and gating thresholds\nweigh it.",
      "type": "integer",
      "format": "uint32",
      "minimum": 0
    }
  },
  "additionalProperties": false,
  "required": [
    "name",
    "weight",
    "setup",
    "expect"
  ],
  "$defs": {
    "Expect": {
      "description": "What the run must produce.",
      "type": "object",
      "properties": {
        "calls": {
          "description": "How many times each faked binary must have been called. A count of `0` asserts a\ndependency was **not** touched, which is often the interesting half.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "integer",
            "format": "uint",
            "minimum": 0
          }
        },
        "event_counts": {
          "description": "How many events of each type must have been emitted. A count of `0` proves one never\nhappened.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "integer",
            "format": "uint",
            "minimum": 0
          }
        },
        "events": {
          "description": "Structured events that must appear, in order.\n\nMatched as a **subsequence**: other events may occur in between, and each entry only\nconstrains the fields it names. An exact list would break the day the subject gains one\nnew event.",
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": true
          }
        },
        "exec": {
          "description": "A project executable that checks what the core cannot.\n\nIt receives the observations as JSON on its standard input and answers\n`{\"ok\": bool, \"diffs\": [...]}`. See `docs/hooks.md`.",
          "type": [
            "string",
            "null"
          ]
        },
        "exit_code": {
          "description": "The exit code the subject must return.",
          "type": [
            "integer",
            "null"
          ],
          "format": "int32"
        },
        "files": {
          "description": "Assertions on the files the subject wrote, by path.\n\nA path may use the variables isolation defines — `$HOME`, `${XDG_CONFIG_HOME}` and the\nrest — or a leading `~`. Anything else is refused rather than left literal, because a\nstray `$TYPO` would make an `absent` assertion trivially true.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "$ref": "#/$defs/TextExpectation"
          }
        },
        "invariants": {
          "description": "Named invariants that must hold. The names come from the project configuration, so a\nname nothing declares is a case failure rather than a silent skip.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "stderr": {
          "description": "Assertions on standard error.",
          "anyOf": [
            {
              "$ref": "#/$defs/TextExpectation"
            },
            {
              "type": "null"
            }
          ]
        },
        "stdout": {
          "description": "Assertions on standard output.",
          "anyOf": [
            {
              "$ref": "#/$defs/TextExpectation"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "Match": {
      "description": "Criterion selecting a rule. Every field is optional; a `Match` whose fields are\nall absent is the **catch-all**.\n\nDeliberately without `deny_unknown_fields`: a project composes its own criterion\non top of this one —\n`struct MyMatch { #[serde(flatten)] core: Match, agent: Option<String> }` — and\n`flatten` is incompatible with rejecting unknown fields. Catching typos is the\ncore's job, at case load time, against the JSON schema.",
      "type": "object",
      "properties": {
        "args_contain": {
          "description": "Substring searched for in the arguments joined by spaces.",
          "type": [
            "string",
            "null"
          ]
        },
        "bin": {
          "description": "Name of the faked binary, as the caller spelled it.",
          "type": [
            "string",
            "null"
          ]
        },
        "call": {
          "description": "Rank of the call, 1-indexed, for the counter key in force.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "stdin_contains": {
          "description": "Substring searched for in standard input.",
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "Rule": {
      "description": "One rule: a criterion and the response that goes with it.\n\nDeliberately not generic over the criterion. A project needing its own criterion\ndeclares its own `Rule` — it has its own response fields anyway — and reuses only\n[`Match`] from this crate, flattened under its own. Making `Rule` generic would\nmean carrying serde and schemars bounds on the parameter for a benefit nobody is\nasking for.",
      "type": "object",
      "properties": {
        "exec": {
          "description": "`real` passes through to the real binary, found further along `PATH`. Any\nother value is the path of a project executable to delegate to, with the same\narguments.",
          "type": [
            "string",
            "null"
          ]
        },
        "exit": {
          "description": "Exit code of the fake. Absent means `0`.",
          "type": [
            "integer",
            "null"
          ],
          "format": "int32"
        },
        "headers": {
          "description": "Response headers, for the HTTP door. Ignored by the binary door.\n\nPresent because a client that refuses a body without its `Content-Type` is not a\nclient doing anything unusual, and a fake it rejects tests nothing.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "latency_ms": {
          "description": "Wait before responding, in milliseconds. Meant to reproduce a\ntiming-sensitive sequence, not to slow things down for its own sake.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "match": {
          "description": "The criterion deciding whether this rule applies.",
          "$ref": "#/$defs/Match",
          "default": {}
        },
        "status": {
          "description": "HTTP status, for a dependency faked behind the HTTP door. Absent means `200`.\n\nSeparate from `exit` rather than reusing it: an exit code is a number between 0 and\n255 that means \"did the program succeed\", and a status is a three-digit code that\nmeans something else entirely. Folding them together would make `exit: 404`\nmeaningless at the binary door and `exit: 1` meaningless at this one.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0
        },
        "stderr": {
          "description": "What the fake writes on its standard error.",
          "type": [
            "string",
            "null"
          ]
        },
        "stdout": {
          "description": "What the fake writes on its standard output.",
          "type": [
            "string",
            "null"
          ]
        }
      }
    },
    "Scenario": {
      "description": "The whole scenario, as it reaches the fake binary.",
      "type": "object",
      "properties": {
        "render": {
          "description": "Executable that dresses the selected response. It receives the rule and the\ncall as JSON on its standard input, and returns the bytes to emit.",
          "type": [
            "string",
            "null"
          ]
        },
        "rules": {
          "description": "The rules, in the order they are tried.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/Rule"
          }
        }
      },
      "required": [
        "rules"
      ]
    },
    "Setup": {
      "description": "How to prepare and invoke the subject.\n\nThe core understands exactly two keys, `run` and `exec`. **Everything else is\nopaque** and travels untouched into the setup hook, which is what lets a project\nwrite its own vocabulary here without the core learning any domain words.",
      "type": "object",
      "properties": {
        "exec": {
          "description": "A project executable that prepares the isolated directory. It receives this whole\nblock as JSON on its standard input.",
          "type": [
            "string",
            "null"
          ]
        },
        "run": {
          "description": "The command line to invoke, argument by argument. No shell is involved, so there\nare no quoting rules to remember.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": true
    },
    "Step": {
      "description": "One exchange with the subject.",
      "type": "object",
      "properties": {
        "expect": {
          "description": "What this exchange must produce.",
          "$ref": "#/$defs/Expect",
          "default": {}
        },
        "name": {
          "description": "What this exchange is for, in the reader's words.\n\nOptional, and worth writing: it is what a failure names instead of an index alone, so a\nreader is not left counting lines in the document to find which one broke.",
          "type": [
            "string",
            "null"
          ]
        },
        "request": {
          "description": "How to perform this exchange. Opaque to the core, exactly like `setup` past `run`.\n\n*How many* exchanges there are is the format's business; *how* one is performed belongs to\nwhichever adapter claims the case.",
          "type": "object",
          "additionalProperties": true
        }
      },
      "additionalProperties": false
    },
    "TextExpectation": {
      "description": "Assertions on a stream of text.",
      "type": "object",
      "properties": {
        "absent": {
          "description": "Substrings that must appear nowhere. This is the family that catches an\nunresolved variable or a leaked secret.",
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        },
        "contains": {
          "description": "Substrings that must all appear.",
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    }
  }
}